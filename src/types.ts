export type Credentials = {
  username: string;
  password: string;
};

export type User = {
  id: number,
  firstName: string,
  lastName: string,
  email: string,
  role: string,
  createdAt: string,
  updatedAt: string
}